#!/bin/bash
torrent_path=$1
if [ -z "$torrent_path" ] && [ -d /media/transmission/torrents ]
then
	torrent_path=/media/transmission/torrents
fi
for file in "$HOME/Downloads/"*.torrent
do
	[ -f "$file" ] && cp "$file" "${torrent_path}/${file##*/}" && rm "$file"
done
